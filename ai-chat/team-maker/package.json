{
  "name": "team-maker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "bun --env-file=../.env run src/server.ts",
    "dev": "bun --env-file=../.env --watch run src/server.ts",
    "cli": "bun --env-file=../.env run src/cli.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
