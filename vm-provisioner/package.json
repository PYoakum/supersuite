{
  "name": "vm-provisioner",
  "version": "1.0.0",
  "description": "Provision Firecracker, QEMU, and Docker Compose workloads with a real-time dashboard",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "bun run server.js",
    "dev": "bun --watch run server.js"
  }
}
